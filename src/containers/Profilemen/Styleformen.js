import { makeStyles } from "@material-ui/core/styles";


export const useStyles = makeStyles({
  bgprofile: {
    backgroundImage: "url(bgprofile.png)",
    backgroundSize: "100vw 100vh",
    minHeight: "100vh",
    height: "auto",
    width: "auto",
    display: "flex",
  },
});